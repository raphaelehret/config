# Set this to true if you want to keep the screenshot images 
# on your tmp folder instead of deleting them
KEEP=true

# Path to the temporary screenshots folder
TMP=~/tmp/mypuush-screenshots/

# Name of the temp file, using date (by default day-month-year@hour-minute-second) 
NAME=`date +"%d-%m-%y@%H-%M-%S"`.png

# Final path to the screenshot temporary file
FILE=$TMP/$NAME

# Path to the links file
LINKFILE=$TMP/links

# Path to the folder where the images are saved (KEEP option)
SAVEFOLDER=~/Images/screens
SAVEPATH=$SAVEFOLDER/$NAME
